# Gateway discovery report

Gateway: `https://us.apigw.jamf.com`

Read-only integration.

`403` does NOT mean "path correct, scope missing". An integration granted every
available `read:pro:*` scope still receives 403 on several routes, and
`blueprints` list returns 200 under the same scope that 403s on
`blueprints/components`. Treat a 403 row as UNCONFIRMED until the negative
controls below say otherwise:

- `_control-bogus-route` — a route that cannot exist, under a working service.
  403 here means BAD_PERMISSIONS marks an unknown route, so every other 403 row
  is a wrong path rather than a permission gap.
- `_control-bogus-service` — a service that cannot exist. Expect 404.

Tenant identifiers and result counts are deliberately absent: this file is
committed and shared externally. The envelope column lists response *key names*,
which is the part that matters for writing a pagination helper.

## Service segments the gateway hosts

Determined by probing a route that cannot exist against each candidate:
403 BAD_PERMISSIONS means the segment is real, 404 means it is not.

| candidate | hosted |
|---|---|
| `ddm/report` | yes |
| `proclassic` | yes |
| `blueprints` | yes |
| `devices` | yes |
| `device-groups` | yes |
| `pro` | yes |
| `compliance-benchmarks` | ? (403) |
| `device-actions` | yes |
| `declarations` | no |
| `declaration-reporting` | no |
| `device-management-actions` | no |
| `classic` | no |
| `jamf-pro` | no |
| `jamf-pro-classic` | no |
| `jamf-pro-api` | no |
| `jssresource` | no |
| `protect` | no |
| `security-cloud` | no |
| `benchmarks` | no |
| `compliance` | no |
| `mscp` | no |
| `users` | no |
| `computers` | no |
| `mobile-devices` | no |
| `inventory` | no |
| `patch` | no |
| `policies` | no |
| `ddm` | no |
| `declarative` | no |
| `device-declarations` | no |
| `reporting` | no |
| `declaration-reports` | no |
| `jamf-protect` | no |
| `jamf-security-cloud` | no |
| `settings` | no |
| `self-service` | no |

## Tenant-context header candidates

Probed against `/api/pro/v1/device-declarations`, which answers 400
`REQUEST_CONTEXT_NOT_PROVIDED` without a tenant in the path. Anything other
than a repeated 400 means the header was understood.

| header | result |
|---|---|
| `X-Jamf-Tenant-Id` | ignored (still 400) |
| `X-Jamf-TenantId` | ignored (still 400) |
| `X-Jamf-Tenant` | ignored (still 400) |
| `X-Tenant-Id` | ignored (still 400) |
| `X-TenantId` | ignored (still 400) |
| `Tenant-Id` | ignored (still 400) |
| `Jamf-Tenant-Id` | ignored (still 400) |
| `Jamf-Tenant` | ignored (still 400) |
| `X-Jamf-Request-Context` | ignored (still 400) |
| `X-Request-Context` | ignored (still 400) |

## Routes

| group | resolved segment | status | url | notes |
|---|---|---|---|---|
| `_control-bogus-route` | — | 403 | `/api/devices/v1/tenant/{TENANT}/zz-no-such-route-control` | 1 combos tried; services: devices |
| `_control-bogus-service` | — | 404 | `/api/zz-no-such-service-control/v1/tenant/{TENANT}/things` | 1 combos tried; services: zz-no-such-service-control |
| `_control-bogus-flat-route` | — | 400 | `/api/pro/v1/zz-no-such-flat-route-control` | 1 combos tried; services: pro |
| `blueprints` | `blueprints` | 200 | `/api/blueprints/v1/tenant/{TENANT}/blueprints` | style `tenant`, envelope: `totalCount` |
| `blueprint-components` | `blueprints` | 200 | `/api/blueprints/v1/tenant/{TENANT}/blueprint-components` | style `tenant`, envelope: `totalCount` |
| `devices` | `devices` | 200 | `/api/devices/v1/tenant/{TENANT}/devices` | style `tenant`, envelope: `page, pageSize, totalCount, totalPages, hasNext, hasPrevious` |
| `device-groups` | `device-groups` | 200 | `/api/device-groups/v1/tenant/{TENANT}/device-groups` | style `tenant`, envelope: `page, pageSize, totalCount, totalPages, hasNext, hasPrevious` |
| `compliance-benchmarks` | — | 500 | `/api/compliance-benchmarks/v1/tenant/{TENANT}/benchmarks` | 1 combos tried; services: compliance-benchmarks |
| `jamf-pro-classic` | `proclassic` | 200 | `/api/proclassic/tenant/{TENANT}/activationcode` | style `raw`, envelope: `activation_code` |
| `declaration-reporting` | `ddm/report` | 200 | `/api/ddm/report/v1/tenant/{TENANT}/devices/{DEVICE}/channels` | style `tenant`, envelope: `deviceId, channels` |
| `jamf-pro` | `pro` | 200 | `/api/pro/v1/tenant/{TENANT}/account-groups` | style `tenant`, envelope: `totalCount` |
| `jamf-pro-api-buildings` | `pro` | 200 | `/api/pro/v1/tenant/{TENANT}/buildings` | style `tenant`, envelope: `totalCount` |

Generated by `scripts/discover-gateway.sh`.
Raw responses are in `fixtures/raw/` and are gitignored — they contain live
device data. Committed shapes in `fixtures/shapes/` carry type names only.
